<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nauger.misgastosdiarios">

    <!-- Define la configuración base del proyecto y su identificador de paquete -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:usesCleartextTraffic="false">

        <activity android:name=".ExpenseDetailActivity" />

        <!-- Actividad principal que se ejecuta al iniciar la aplicación -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Indica que esta actividad es el punto de entrada de la app y aparece en el launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
